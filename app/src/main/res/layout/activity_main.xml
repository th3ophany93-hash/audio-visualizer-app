<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/background">

    <com.audiovisualizer.render.gl.VisualizerSurfaceView
        android:id="@+id/visualizerSurface"
        android:layout_width="0dp"
        android:layout_height="0dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBottom_toTopOf="@id/timelineRow" />

    <!-- UX spec section 3: a compact, navigational-only scrubber - never part of the export. -->
    <LinearLayout
        android:id="@+id/timelineRow"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:paddingStart="4dp"
        android:paddingTop="2dp"
        android:paddingBottom="2dp"
        android:paddingEnd="8dp"
        android:background="@color/surface"
        app:layout_constraintTop_toBottomOf="@id/visualizerSurface"
        app:layout_constraintBottom_toTopOf="@id/analysisProgressRow">

        <ImageButton
            android:id="@+id/timelinePlayPauseButton"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="@string/timeline_play_pause"
            android:src="@android:drawable/ic_media_play"
            android:enabled="false" />

        <SeekBar
            android:id="@+id/timelineSeekBar"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:enabled="false" />

    </LinearLayout>

    <!-- A dedicated, non-overlapping row for audio-analysis progress: it pushes the
         controls panel down while visible instead of covering the import buttons
         with a modal dialog. Gone by default, shown only during loadAudio(). -->
    <LinearLayout
        android:id="@+id/analysisProgressRow"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:paddingStart="12dp"
        android:paddingTop="6dp"
        android:paddingBottom="6dp"
        android:paddingEnd="12dp"
        android:background="@color/surface"
        android:visibility="gone"
        app:layout_constraintTop_toBottomOf="@id/timelineRow"
        app:layout_constraintBottom_toTopOf="@id/controlsPanel">

        <TextView
            android:id="@+id/analysisProgressText"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textColor="@color/on_background"
            android:textSize="12sp"
            tools:text="Анализ частот…" />

        <ProgressBar
            android:id="@+id/analysisProgressBar"
            style="?android:attr/progressBarStyleHorizontal"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:max="100"
            android:progress="0" />

    </LinearLayout>

    <LinearLayout
        android:id="@+id/controlsPanel"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:padding="8dp"
        android:background="@color/surface"
        app:layout_constraintTop_toBottomOf="@id/analysisProgressRow"
        app:layout_constraintBottom_toTopOf="@id/layersTitle">

        <com.google.android.material.button.MaterialButton
            android:id="@+id/pickAudioButton"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:layout_marginEnd="4dp"
            android:text="@string/action_pick_audio" />

        <com.google.android.material.button.MaterialButton
            android:id="@+id/pickImageButton"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:layout_marginStart="4dp"
            android:layout_marginEnd="4dp"
            android:text="@string/action_pick_image" />

        <com.google.android.material.button.MaterialButton
            android:id="@+id/exportButton"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:layout_marginStart="4dp"
            android:layout_marginEnd="4dp"
            android:text="@string/action_export" />

        <com.google.android.material.button.MaterialButton
            android:id="@+id/addEffectButton"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:layout_marginStart="4dp"
            android:text="@string/action_add_effect" />
    </LinearLayout>

    <TextView
        android:id="@+id/layersTitle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/surface"
        android:paddingStart="12dp"
        android:paddingTop="8dp"
        android:paddingBottom="4dp"
        android:paddingEnd="12dp"
        android:text="@string/layers_title"
        android:textColor="@color/on_background"
        android:textStyle="bold"
        app:layout_constraintTop_toBottomOf="@id/controlsPanel"
        app:layout_constraintBottom_toTopOf="@id/layersList" />

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/layersList"
        android:layout_width="match_parent"
        android:layout_height="280dp"
        android:background="@color/surface"
        app:layout_constraintTop_toBottomOf="@id/layersTitle"
        app:layout_constraintBottom_toBottomOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>
